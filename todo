0. lib script for paths
  # a path variable for each os
  # addpath call modifies line in file
  # dont track git
  # assign based on os detect
  # determine shell
    # check profile file exist else create
    # check file contains source lib script
    # if no add line to source
OR profile file symlink
1. setup vim
  # download latest dependencies
  # make using latest version deps
  # check vimrc
  # uninstall vim
2. setup tmux
3. setup markdown hotreload
4. learn zsh
5. setup zsh

THOUGHTS
========

SOFTWARE TO INVESTIGATE
  primitive linux commands: sed, grep, 
  kernel
  vim
  zsh
  c lang
  opengl
PROJECTS TO CONTEMPLATE
  functional graphs
  revisit nonograms
  HSL color generation
  rubiks properties
  diatonic music generator / improv chord trainer
  functional filesystems / turtle
  n dimensional minesweeper
  math libraries
  algorithm libraries
  statistical libraries (finally make use of that book)
  bifurcation

vim install script:
DETECT VIM7 uninstall
  sudo rm -rf /usr/share/vim
  sudo rm /usr/bin/vim
  rm ~/.vimrc
  rm ~/.vim
  rm ~/.viminfo
  install vim8 deps < OS specific
  make vim 8

directory structure setup??

make sub OSX installer entertainment package (twitch + spotify), gui devel (adobe + google backup)

ZSH guide
http://zsh.sourceforge.net/Intro/intro_toc.html

pkg removal guide
# https://stackoverflow.com/questions/25925752/uninstall-packages-in-mac-os-x

ANSI & tput
http://ascii-table.com/ansi-escape-sequences.php
http://linux.101hacks.com/ps1-examples/prompt-color-using-tput/

# create symlink
#ln -sf "$BASEDIR/.vimrc" ~/.vimrc
